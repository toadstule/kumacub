# KumaCub Configuration

# Logging configuration
[log]
level = "INFO"  # Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
structured = true  # Whether to use structured (JSON) logging

# Checks configuration
[[checks]]
name = "disk usage"
executor = { command = "echo", args = ["-n", "OK"] }
publisher = { url = "https://uptime-kuma.jibson.com", push_token = "V0LPCbmhJ1?" }

#[[checks]]
#name = "my env"
#executor = { name = "process", command = "/usr/bin/env", env = { YOUR_MOM = "goes to college", YOUR_DAD = "goes to work" } }
#parser = { name = "nagios" }
#publisher = { name = "uptime_kuma", url = "http://your-uptime-kuma-instance.com", push_token = "your-push-token-here" }
#schedule = { interval = 300 }
#
#[[checks]]
#name = "busted"
#executor = { name = "process", command = "/bin/sh", args = ["-c", "false"], env = { YOUR_MOM = "goes to college", YOUR_DAD = "goes to work" } }
#parser = { name = "nagios" }
#publisher = { name = "uptime_kuma", url = "http://your-uptime-kuma-instance.com", push_token = "your-push-token-here" }
#schedule = { interval = 300 }
